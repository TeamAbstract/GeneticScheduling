import util

defaultCleanTime = util.getTimeObject("0:30")  # clean time in hours for schedules of same product
longerCleanTime = util.getTimeObject("1")  # clean time for schedules of different product
